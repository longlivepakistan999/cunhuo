# 宝塔面板部署

适用宝塔 7.x / 9.x。两种方式选一种即可。

---

## 方式 A：用宝塔的「Python 项目管理器」（推荐，最省事）

### 1. 安装 Python 环境

宝塔面板 → 软件商店 → 搜 **「Python 项目管理器」** → 安装。

进入插件 → 版本管理 → 安装 **Python 3.10**（或 3.9+ 任意版本）。

### 2. 上传代码

宝塔 → 文件 → 进入 `/www/wwwroot/`，新建文件夹 `cunhuo` → 上传你的代码（或终端 `git clone`）。

最终目录结构应该是 `/www/wwwroot/cunhuo/app.py`。

### 3. 添加项目

Python 项目管理器 → **添加项目**：

| 字段 | 填写 |
|------|------|
| 项目名称 | `cunhuo` |
| 项目路径 | `/www/wwwroot/cunhuo` |
| Python 版本 | 选刚装的 3.10 |
| 框架 | **Flask** |
| 启动方式 | **gunicorn** |
| 启动文件/文件夹 | `app.py` |
| 入口参数（启动选项） | `-w 1 -b 0.0.0.0:5000 --timeout 120 app:app` |
| 端口 | `5000` |
| 是否安装依赖 | ✅ 勾上（自动跑 `pip install -r requirements.txt`） |

⚠️ **`-w 1` 必须**：队列调度器和实时进度都在进程内存里，多 worker 会重复执行任务。

点 **确认**，等待依赖安装完成（几十秒），项目会自动启动。

### 3.5. 开 Basic Auth 鉴权（强烈建议）

不开的话谁知道 IP:5000 都能用。

项目 → **环境变量**（或 `配置 → 环境变量`）→ 添加两条：

| 变量名 | 值 |
|--------|------|
| `CUNHUO_USER` | `admin`（自己起） |
| `CUNHUO_PASS` | 一串强密码 |

保存 → 项目重启。再访问会先弹浏览器登录框。两个变量任一为空都不会启用鉴权。

⚠️ Basic Auth 走 HTTP 是**明文**（base64 不算加密），生产务必走方式 B 套 HTTPS。

### 4. 放行端口

宝塔 → 安全 → 添加端口规则：**5000 TCP** 放行。

云服务器（阿里云/腾讯云/华为云）还要去**控制台的安全组**里同样放行 5000，否则外网访问不了。

### 5. 访问

浏览器打开 `http://你的服务器IP:5000` 就能用了。

---

## 方式 B：直接套域名 + HTTPS（生产推荐）

在方式 A 基础上加一层 Nginx 反代，用域名 + HTTPS 访问。

### 1. 添加站点

宝塔 → 网站 → 添加站点：

- 域名：填你的域名，例如 `cunhuo.example.com`
- PHP 版本：**纯静态**（我们不要 PHP）
- 数据库：**不创建**

### 2. 配置反向代理

进入刚加的站点 → **反向代理** → 添加反向代理：

| 字段 | 填写 |
|------|------|
| 代理名称 | `cunhuo` |
| 目标 URL | `http://127.0.0.1:5000` |
| 发送域名 | `$host` |

保存后访问 `http://cunhuo.example.com` 应该能打开页面。

### 3. 上传大文件支持

默认 Nginx 限制 1MB 上传，要改大。站点设置 → **配置文件** → 在 `server { }` 里加一行：

```nginx
client_max_body_size 50M;
```

保存，重载 Nginx。

### 4. 申请 SSL

站点设置 → **SSL** → Let's Encrypt → 选域名 → 申请。
申请成功后开 **强制 HTTPS**。

之后用 `https://cunhuo.example.com` 访问。

### 5. 关闭外网 5000 端口

防火墙里**移除** 5000 的对外放行（只留本机 127.0.0.1 用），用户只能通过域名走 Nginx。

---

## 常见问题

### 项目启动失败 / 502 Bad Gateway

Python 项目管理器 → 项目 → **日志** 看报错，多半是：

- 没装依赖：手动进项目目录跑 `/www/server/pyproject_evn/<项目名>_venv/bin/pip install -r requirements.txt`
- 端口被占：`netstat -tlnp | grep 5000`，换个端口（同时改 Nginx 反代和入口参数）
- 没权限写 `jobs/` 或 `jobs.db`：项目根目录授权 755，所有者 `www`

### 怎么停止 / 重启

Python 项目管理器 → 找到项目 → **停止 / 重启** 按钮。

### 数据存哪、怎么备份

只两样东西要备份：

- `/www/wwwroot/cunhuo/jobs.db` ——任务元数据
- `/www/wwwroot/cunhuo/jobs/` ——每个任务的输入和 CSV 结果

宝塔 → 计划任务 → 添加 **备份目录** 任务，目录填 `/www/wwwroot/cunhuo`，定期上传到 OSS / 七牛即可。

### 升级代码

文件管理器替换文件（或 `git pull`）→ Python 项目管理器里点 **重启**。
SQLite 表结构会在启动时自动 `ALTER TABLE` 迁移，不用手动改库。

### 日志在哪

Python 项目管理器 → 项目 → **日志** 标签，gunicorn 的 access log 和 error log 都在这里。

---

## 抗崩说明（重要）

宝塔的 Python 项目管理器自带 **进程守护**：项目挂了会自动拉起。

- 进程突发崩溃 → 自动重启 → `init_db()` 把残留 `running` 标 `interrupted` → worker 接着跑队列里没动过的 `queued` 任务
- 服务器重启 → 项目自动启动（Python 项目管理器默认勾选「开机启动」，没勾的话补勾上）
- 已完成任务的 CSV 结果都在磁盘上，不会丢

详见根目录 `README.md` 的「抗崩说明」章节。
